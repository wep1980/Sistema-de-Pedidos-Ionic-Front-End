// Classe que ira Interagir com o FieldMessage do backEnd que carrega as mensagens de erro de validação. nome do campo e mensagem
export interface FieldMessage {
    fieldName : string;
    message : string;

}